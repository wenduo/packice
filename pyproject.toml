[project]
name = "packice"
version = "0.1.0"
description = "A flexible, batteries-included peer-to-peer cache system with zero-copy data access"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]
